// Experimental slider with minimal JavaScript: Could and should be improved before use, not yet accessible
// The end scroll detection is a bit flakey, so we're converting the number to a positive number (Math.abs) and giving it a ~5px leeway.